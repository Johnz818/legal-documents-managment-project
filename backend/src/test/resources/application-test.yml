spring:
  datasource:
    url: jdbc:mysql://localhost:3306/legal_case_management_test
    username: ${LEGAL_DB_USERNAME}
    password: ${LEGAL_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

legal:
  document:
    storage:
      local:
        root: target/test-document-storage
